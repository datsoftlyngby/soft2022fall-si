DROP TABLE IF EXISTS Exam;
CREATE TABLE Exam(
                         id INT PRIMARY KEY,
                         si VARCHAR(20) NOT NULL,
                         dls VARCHAR(20) NOT NULL,
                         tst VARCHAR(20) NOT NULL
);
